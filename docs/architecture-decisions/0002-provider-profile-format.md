# ADR 0002: Provider Profile Format

## Decision

Provider profiles are JSON package resources with a JSON Schema companion.

## Rationale

JSON is dependency-free, easy to inspect, and sufficient for base URLs, auth environment variables,
capabilities, parameter policies, and output-token parameter rules.
